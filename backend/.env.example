# 이 파일을 .env 로 복사한 뒤 값을 채우세요. .env 는 커밋되지 않습니다.

# OpenAI API 키
OPENAI_API_KEY=sk-...

# Firebase 서비스 계정 키(JSON) 경로.
# 콘솔 > 프로젝트 설정 > 서비스 계정 에서 새로 발급받아 사용하세요.
FIREBASE_CREDENTIALS=app/firebase_key.json
